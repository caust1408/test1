// Craig Austgen
// file 2 

// declares a function with the variable name cat
var cat = function() {
	// all the function does it print out cat
	print('cat');
}
// creates a declaritive function with one parameter
function dog(treat) {
	// prints parameter with strings
	print('I want ' + treat + ' treats');
}

dog(5);	// calls dog function

// calls cat function
cat();
