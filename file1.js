// Craig Austgen
// file # 1
//

// creates variable cmp and sets it to 237
var cmp = 237;
// if cmp equals 237 then change cmp to 344
 if (cmp == 237) {
	 // sets cmp to 344
	 cmp = 344;
	 // prints it out
	 print('cmp has changed to ' + cmp);
 }
